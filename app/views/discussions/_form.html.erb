<%= form_for(@discussion) do |f| %>
  <% if @discussion.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(@discussion.errors.count, "error") %> prohibited this discussion from being saved:</p>

      <ul>
      <% @discussion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="new-discussion">
<%= f.fields_for :comments do |ff| %>
<%= ff.label :content %>
<%= ff.text_area :body, placeholder: "Enter your message", class: "form-text" %>
<% end %>

    <%= f.submit class: "button", data: {disable_with: "Saving..."} %>
      </div>
</div>
<% end %>
